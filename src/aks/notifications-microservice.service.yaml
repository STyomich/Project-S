apiVersion: v1
kind: Service
metadata:
  name: notifications-microservice
  namespace: project-s-namespace
spec:
  selector:
    app: notifications-microservice
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
